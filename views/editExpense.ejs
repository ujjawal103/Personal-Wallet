<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit-Expense</title>
    <link rel="stylesheet" href="/styleSheets/initial.css">
    <style>
        textarea{
            width: 50vw;
            height: 40vh;
            background-color: grey;
            color: whitesmoke;
            font-size: 4vw;
            font-style: italic;
            text-wrap: wrap;
            display: flex;
            flex-wrap: wrap;
            border-radius: 20px;
            border: 3px solid red;
            padding: 10px;
            cursor: pointer;
        }
        textarea::placeholder{
            color: whitesmoke;
            font-size: 4vw;
            font-style: italic;
            text-wrap: wrap;
            display: flex;
            flex-wrap: wrap;
            margin: 10px;
        }
    </style>
</head>
<body>
    
    <form method="post" action="/expenses/expense/<%=expense.id%>/edit">
        <h2>Expense Amount :- </h2>
        <input type="number" placeholder="₹ Amount" min="0" name="loss" id="initialMoney" value="<%= expense.loss %>" required>
        <div><textarea name="reason" id="reason" rows="6" cols="12" ><%= expense.reason %></textarea></div>
        <button type="submit"> Submit </button>
    </form>

</body>
</html>