<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New-Income</title>
    <link rel="stylesheet" href="/styleSheets/initial.css">
    <style>
        textarea{
            width: 50vw;
            height: 40vh;
            background-color: grey;
            color: whitesmoke;
            font-size: 4vw;
            font-style: italic;
            text-wrap: wrap;
            display: flex;
            flex-wrap: wrap;
            border-radius: 20px;
            border: 3px solid rgb(23, 205, 63);
            padding: 10px;
            cursor: pointer;
        }
        textarea::placeholder{
            color: whitesmoke;
            font-size: 4vw;
            font-style: italic;
            text-wrap: wrap;
            display: flex;
            flex-wrap: wrap;
            margin: 10px;
        }
    </style>
</head>
<body>
    
    <form method="post" action="/expenses/income">
        <h2>Income Amount :- </h2>
        <input type="number" placeholder="₹ Amount" min="0" name="gain" id="initialMoney" required>
        <div><textarea name="reason" id="reason" rows="6" cols="12" placeholder="Enter your Reason To Profit.."></textarea></div>
        <button type="submit"> Submit </button>
    </form>

</body>
</html>